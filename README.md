# big_data

# 설치
1. 가상환경 설치
    ```
    conda create --name new_env --file requirements.txt
    ```

2. 자바 설치
   ```
   sudo apt update
   sudo apt install openjdk-17-jdk -y
   ```

# 실행
    python -m test
    
